@model IEnumerable<ProductsProcess.EF.Product>
@{
    ViewBag.Title = "ViewCart";
}

<h2>ViewCart</h2>
<a href="/Customer/ViewProduct/" class="btn btn-danger">Back to Product</a>


<table class="table">
    <tr>
        <th>Product Name</th>
        <th>Description</th>
        <th>Price</th>
        <th>Delete</th>
    </tr>
    @foreach (var product in Model)
    {
        <tr>
            @*<td>@product.ProductID</td>*@
            <td>@product.Name</td>
            <td>@product.Description</td>
            <td>@product.Price</td>
            <td>@Html.ActionLink("Delete", "CancelFromCart", new { id = product.ProductID }, new { @class = "btn btn-danger" })</td>
            @*<td>
                <input type="number" name="quantity" value="1" min="1" class="btn btn-outline-secondary"/>
            </td>*@
        </tr>
    }
</table>
